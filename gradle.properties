org.gradle.daemon=true
org.gradle.jvmargs=-Xms128m -Xmx1500m
org.gradle.configureondemand=false
android.enableR8.fullMode=false

# NOTE: org.gradle.java.home intentionally NOT set here so this file is
# portable across local dev (Windows/macOS/Linux) and CI runners. Configure
# the build's JVM via JAVA_HOME or a gradle toolchain instead. If you need a
# machine-specific override, create gradle.properties.local (gitignored).

